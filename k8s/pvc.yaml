apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: django-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
  storageClassName: managed-csi  # from "standard" to "managed-csi"